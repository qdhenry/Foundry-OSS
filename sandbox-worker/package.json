{
  "name": "migration-sandbox-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --port 8788",
    "dev:remote": "wrangler dev --remote --port 8788",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@foundry/types": "workspace:*",
    "@cloudflare/sandbox": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260214.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.56.0"
  }
}
